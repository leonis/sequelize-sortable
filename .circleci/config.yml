version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/node:7.10
      - image: postgres:9.6
        environment:
          - POSTGRES_USER=tester
          - POSTGRES_DB=ci_test
    working_directory: ~/app

    steps:
      - checkout
      - restore_cache:
          keys:
          - v1-dependencies-{{ checksum "package-lock.json" }}
      - run:
          name: Install dependencies
          command: npm install
      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package-lock.json" }}
      - run:
          name: lint & test
          command: npm run ci
          environment:
            TEST_DB: "postgres://tester@localhost:5432/ci_test"
